// export const serverHost = "http://192.168.2.250:8080";
export const serverHost = "http://localhost:8080";
export const serverURL = `${serverHost}/api/`;

export const config = {
  serverURL,
  serverHost,
};

// This setup is default config. If need/wants environment just setup here to specify if Production or Development
